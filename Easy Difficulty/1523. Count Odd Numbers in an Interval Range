class Solution(object):
    def countOdds(self, low, high):
        n = (high - low) + 1
        if n % 2 == 0:
            return n / 2
        if high % 2 != 0 or low % 2 != 0:
            return (n / 2) + 1
        return n / 2
